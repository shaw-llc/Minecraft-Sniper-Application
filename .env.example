# Minecraft Username Sniper - Environment Variables Example
# Rename this file to .env to use these settings

# You don't need to provide these credentials manually
# The tool will open a browser for Microsoft authentication automatically
# These are only used if you want to avoid the browser authentication

# Microsoft account credentials
EMAIL=your_microsoft_email@example.com
PASSWORD=your_microsoft_password

# API settings (optional)
# Adjust if needed to avoid rate limiting
BASE_DELAY=1.5
MAX_THREADS=5 